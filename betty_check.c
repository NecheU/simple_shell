#include "shellheader.h"

/**
 * main - Code that passes betty check
 *
 * Return: 0
 */

int main(void)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		printf("%c\n", alph);
	}
	return (0);
}
